# Optional: origin for server-side fetches to this app’s API routes.
# Use when deploying somewhere other than Vercel, or when the default
# (Vercel URL, or http://127.0.0.1:$PORT locally) is wrong.
# INTERNAL_API_ORIGIN=https://your-production-domain.com
